// 公众号关注
export enum PUBLIC_STATUS {
  no = '0',
  yes = '1',
}

// 角色状态
export enum ROLE_STATUS {
  taker = '1',
  dispatcher = '2',
}

// 实名认证
export enum VERIFY_STATUS {
  no = '0',
  yes = '1',
}

// 登录状态
export enum LOGIN_STATUS {
  unknown = -1,
  no = 0,
  yes = 1,
}
